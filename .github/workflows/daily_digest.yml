name: Weekly Digest

on:
  pull_request:
    branches:
      - main
      - DEV
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Run the pipeline without sending email"
        required: false
        default: false
        type: boolean
  repository_dispatch:
    types:
      - run-daily-digest

permissions:
  contents: read

concurrency:
  # PR runs are isolated per branch and cancellable; production runs are serialised
  group: dpns-${{ github.event_name == 'pull_request' && github.ref || 'production' }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  run-digest:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
      BRAVE_SEARCH_API_KEY: ${{ secrets.BRAVE_SEARCH_API_KEY }}
      AGENTMAIL_API_KEY: ${{ secrets.AGENTMAIL_API_KEY }}
      AGENTMAIL_INBOX_ID: ${{ secrets.AGENTMAIL_INBOX_ID }}
      EMAIL_FROM: ${{ secrets.EMAIL_FROM }}
      FEEDBACK_EMAIL: ${{ secrets.FEEDBACK_EMAIL }}
      FEEDBACK_SUBJECT: ${{ secrets.FEEDBACK_SUBJECT }}
      FEEDBACK_URL: ${{ secrets.FEEDBACK_URL }}
      LOG_LEVEL: ${{ vars.LOG_LEVEL || 'INFO' }}
      DRY_RUN: ${{ github.event_name == 'pull_request' || (github.event_name == 'workflow_dispatch' && inputs.dry_run) || (github.event_name == 'repository_dispatch' && github.event.client_payload.dry_run == true) }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .

      - name: Run pipeline
        run: python -m src.main
